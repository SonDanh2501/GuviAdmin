export const getOrderSelector = (state) => state.order.data;
export const getOrderTotal = (state) => state.order.totalItem;

export const searchOrderSelector = (state) => state.order.dataSearch;
export const searchOrderTotal = (state) => state.order.totalDataSearch;
